package main

import (
	"fmt"
)

func main() {
	student := make([]string, 35)
	students := make([][]string, 35)
	student[0] = "Michael"
	//student = appende(student, "Michael")
	fmt.Println(student)
	fmt.Println(students)
}
